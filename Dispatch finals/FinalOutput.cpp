

#include <iostream>// iostream is js iostream
#include <unistd.h>// function for typewrite 
#include <string>//function for thread
#include <thread>//function for pause
#include <chrono>// declaration of seconds and milliseconds
#include <limits>// for invalid inputs
using namespace std;
int blonde_blazerInterest = 0;
int Flambae_Anger_Level = 0;
bool isMercyful = false;
bool isRomanticTension = false;
bool isRobertInjured = false;
void typewrite(string text, int speed = 50000){// speed of the typewrite functon
    for ( char c : text){//typewrite function
        cout << c << flush;
        
        usleep(speed);
    }
}
void scene_apartment_interrogation();//scene 1

void warehouse();//scene 2

void news();//scene 3

void streetfight();//scene 4

void Bar();//scene 5

void Billboard();//scene 6


int main(){
  char again;
  
  scene_apartment_interrogation();//goes through storyline first before choices to give it a more gamey 
  warehouse();
  news();
  Bar();
  streetfight();
  Billboard();
do{//loop for choices
int choice;

  std::cout<<"==========LOBBY=========="<<endl;//used std because for some reason cout becomes ambigous
  std::cout<<"Dispatch cover by Aljosh Mendoza & Kent Abillion"<<endl;
  std::cout<<"0 = full story run"<<endl;
  std::cout<<"1 = Interrogation"<<endl;
  std::cout<<"2 = Warehouse"<<endl;
  std::cout<<"3 = News"<<endl;
  std::cout<<"4 = Streetfight"<<endl;
  std::cout<<"5 = Bar"<<endl;
  std:: cout<<"6 = Billboard"<<endl;
  cout<<"7 = Exit"<<endl;
  cout<<" "<<endl;
  
  cin>>choice;

   if (std::cin.fail()) {// for errors so the whole program doesn't crash out like i did......
            std::cin.clear();  // Clear error flag
            std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');  // Discard bad input
            std::cout << "Invalid input. Please enter a number." << std::endl;
            continue;  // Restart the loop
        }
  
  switch(choice){
  case 0:
  scene_apartment_interrogation();
  warehouse();
  news();
  streetfight();
  Bar();
  Billboard();
  break;

  case 1:
  scene_apartment_interrogation();
  break;
  
  case 2:
  warehouse();
  break;
  
  case 3:
  news();
  break;
  
  case 4:
  streetfight();
  break;
  
  case 5:
  Bar();
  break;
  
  case 6:
  Billboard();
  break;
  
  case 7:
  cout<<"Thank you for playing."<<endl;
  return 0;//return 0 so it doesn't continue to try again
  break;

  default:
  cout<<"invalid Command"<<endl;
  }
  
  cout<<"do you want to try again? (y/n)";
  cin>>again;
  }
  while (again == 'y' || again == 'Y');//ends loop here and function if user wants to try again
  cout<<"Thank you for playing."<<endl;
  
    return 0;
}
//this is where all of the scenes are in
void scene_apartment_interrogation(){
   typewrite("\nWelcome to Dispatch");
   this_thread::sleep_for(chrono::seconds(1));
   typewrite("\nExclusively made by Aljosh Mendoza");
   this_thread::sleep_for(chrono::seconds(1));
      typewrite("\nDocumented and flowchart by Kent Steven Abillion");
   this_thread::sleep_for(chrono::seconds(1));
   typewrite("\nNOTE: input a command not among the choices will result in bad outcomes. . .");
   this_thread::sleep_for(chrono::seconds(1));
   cout<<"."<<endl;
   this_thread::sleep_for(chrono::seconds(1));
   cout<<"."<<endl;
   this_thread::sleep_for(chrono::seconds(1));
   cout<<"."<<endl;
   this_thread::sleep_for(chrono::seconds(1));
   cout<<"."<<endl;
   this_thread::sleep_for(chrono::seconds(1));
   typewrite("\nRobert: Obviously, I love him. He's my Dad. ");
    this_thread::sleep_for(chrono::seconds(2));// delay command for each outputs
    typewrite("\nSoothing Voice: Hmm...");
    this_thread::sleep_for(chrono::seconds(2));
    typewrite("\nRobert: What does that hmm mean? Just say what you wanna say? ");
    this_thread::sleep_for(chrono::seconds(2));
    typewrite("\nSoothing voice: It's just, When you add obviously it feels a little less obvious. ");
    this_thread::sleep_for(chrono::seconds(2));
    typewrite("\nSoothing Voice: So, I'll ask again...");
    this_thread::sleep_for(chrono::seconds(2));
    typewrite("\nSoothing Voice: How do you really feel about your father? ");
    this_thread::sleep_for(chrono::seconds(2));
    cout<<" "<<endl;
    char a, w;
    cout<<"A = I love my dad."<<endl;
   
    cout<<"W = Didn't know him well."<<endl;
    
    cout<<"D = He's gone"<<endl;
    cout<<" ";
    cin>>a;

     if (a == 'A' || a =='a'){
        typewrite("\nSoothing voice: It's as if, maybe, It's a little more complicated than you feel comfortable admitting.. ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\n Which is completely understandable.");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\nRobert: look, I love my dad, alright? I love my dad. That's the truth. ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\nSoothing voice: loved ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\nRobert: That's what i said. ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\nSoothing voice: you said loved... ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\n Your dad's dead, so you don't love him, you loved him. ");
        this_thread::sleep_for(chrono::seconds(2));
        typewrite("\nRobert: why would i be trying to find the man who killed him if I didn't love him? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Why would we be in any of this— this— ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nNose starts to bleed*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: shit. My nose is bleeding. Thought i was getting emotionally snotty.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSoothing voice: No, it's probably from when i kicked you in the face.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: yeah. Thanks for reminding me. So Here's gonna happen. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n I'm going to hang you off the side of this building, and you're gonna tell me where shroud is, or ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n i'm going to drop you to your death. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Come on, I thought we were having a breakthrough. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: Hey, we did. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Ya know, for being a real piece of shit, you're pretty easy to talk to. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n But the only breakthrough I need right now, is the location of shroud. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: I'm not telling you shit you fuckin' loser. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: See. This is more like, how I thought this would go...");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: I hope shroud kills you just like he killed your father");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I hope he fuckin' spanks your little daddy issue bitch ass to death. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Are we outside? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n It smells like what outside smells");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Fuckfuckfuck- wait! Wait! Wait! Wait");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey! Hey! I have neighbors");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Use your inside voice and just tell me where shroud is? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Okay, okay, uh uh- Steel- Steel Mill. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him again*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Llewelyn uh, Llewelyn Steel Works okay? Fuck. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: You're sure about that? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Yes! Please, that's all i know! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Stop screaming, goddamnit");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey, hey, I will pull you up if you quiet the fuck down. ");
         this_thread::sleep_for(chrono::seconds(3));
         cout<<" "<<endl;
    }
     else if(a == 'W' || a == 'w'){
        typewrite("\nSoothing voice: It's as if, maybe, It's a little more complicated than you feel comfortable admitting.. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\n Which is completely understandable.");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: Didn't really know him well... he wasn't around much");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nSoothing voice: Well, you're gonna see him even less now, cause he's dead. ");
        this_thread::sleep_for(chrono::seconds(4));
                 typewrite("\nRobert: why would i be trying to find the man who killed him if I didn't love him? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Why would we be in any of this— this— ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nNose starts to bleed*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: shit. My nose is bleeding. Thought i was getting emotionally snotty.");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: No, it's probably from when i kicked you in the face.  ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: yeah. Thanks for reminding me. So Here's gonna happen. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n I'm going to hang you off the side of this building, and you're gonna tell me where shroud is, or ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n i'm going to drop you to your death. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Come on, I thought we were having a breakthrough. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: Hey, we did. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Ya know, for being a real piece of shit, you're pretty easy to talk to. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n But the only breakthrough I need right now, is the location of shroud. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: I'm not telling you shit you fuckin' loser. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: See. This is more like, how I thought this would go...");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: I hope shroud kills you just like he killed your father");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I hope he fuckin' spanks your little daddy issue bitch ass to death. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Are we outside? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n It smells like what outside smells");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Fuckfuckfuck- wait! Wait! Wait! Wait");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey! Hey! I have neighbors");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Use your inside voice and just tell me where shroud is? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Okay, okay, uh uh- Steel- Steel Mill. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him again*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Llewelyn uh, Llewelyn Steel Works okay? Fuck. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: You're sure about that? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Yes! Please, that's all i know! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Stop screaming, goddamnit");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey, hey, I will pull you up if you quiet the fuck down. ");
         this_thread::sleep_for(chrono::seconds(3));
         cout<<" "<<endl;
     }
     else if(a == 'D'|| a == 'd'){
        typewrite("Soothing voice: It's as if, maybe, It's a little more complicated than you feel comfortable admitting.. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\n Which is completely understandable.");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: Look... he's gone. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nSoothing voice: That's a bit of an understatement... ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: How so? ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nSoothing voice: well, he's more than gone, cause he's fuckin' dead. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\n That's the most gone you can be. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: just so sensitive. ");
        this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n Listen, if i didn't love him, we wouldn't be here, right? ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nSoothing voice: we don't have to get into it. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: nope we're in it now. ");
        this_thread::sleep_for(chrono::seconds(4));
                 typewrite("\nRobert: why would i be trying to find the man who killed him if I didn't love him? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Why would we be in any of this— this— ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nNose starts to bleed*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: shit. My nose is bleeding. Thought i was getting emotionally snotty.");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: No, it's probably from when i kicked you in the face.  ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: yeah. Thanks for reminding me. So Here's gonna happen. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n I'm going to hang you off the side of this building, and you're gonna tell me where shroud is, or ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n i'm going to drop you to your death. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Come on, I thought we were having a breakthrough. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: Hey, we did. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Ya know, for being a real piece of shit, you're pretty easy to talk to. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n But the only breakthrough I need right now, is the location of shroud. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: I'm not telling you shit you fuckin' loser. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: See. This is more like, how I thought this would go...");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: I hope shroud kills you just like he killed your father");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I hope he fuckin' spanks your little daddy issue bitch ass to death. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Are we outside? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n It smells like what outside smells");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Fuckfuckfuck- wait! Wait! Wait! Wait");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey! Hey! I have neighbors");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Use your inside voice and just tell me where shroud is? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Okay, okay, uh uh- Steel- Steel Mill. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him again*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Llewelyn uh, Llewelyn Steel Works okay? Fuck. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: You're sure about that? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Yes! Please, that's all i know! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Stop screaming, goddamnit");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey, hey, I will pull you up if you quiet the fuck down. ");
         this_thread::sleep_for(chrono::seconds(3));
         cout<<" "<<endl;
        

     }
     else {
       typewrite("\nSoothing voice: It's as if, maybe, It's a little more complicated than you feel comfortable admitting.. ");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\n Which is completely understandable.");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nRobert: Didn't really know him well... he wasn't around much");
        this_thread::sleep_for(chrono::seconds(4));
        typewrite("\nSoothing voice: Well, you're gonna see him even less now, cause he's dead. ");
        this_thread::sleep_for(chrono::seconds(4));
                 typewrite("\nRobert: why would i be trying to find the man who killed him if I didn't love him? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Why would we be in any of this— this— ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nNose starts to bleed*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: shit. My nose is bleeding. Thought i was getting emotionally snotty.");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: No, it's probably from when i kicked you in the face.  ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: yeah. Thanks for reminding me. So Here's gonna happen. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n I'm going to hang you off the side of this building, and you're gonna tell me where shroud is, or ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n i'm going to drop you to your death. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Come on, I thought we were having a breakthrough. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nRobert: Hey, we did. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Ya know, for being a real piece of shit, you're pretty easy to talk to. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\n But the only breakthrough I need right now, is the location of shroud. ");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nSoothing voice: I'm not telling you shit you fuckin' loser. ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nRobert: See. This is more like, how I thought this would go...");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: I hope shroud kills you just like he killed your father");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I hope he fuckin' spanks your little daddy issue bitch ass to death. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Are we outside? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n It smells like what outside smells");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him*");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Fuckfuckfuck- wait! Wait! Wait! Wait");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey! Hey! I have neighbors");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\n Use your inside voice and just tell me where shroud is? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Okay, okay, uh uh- Steel- Steel Mill. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThreatens to drop him again*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Llewelyn uh, Llewelyn Steel Works okay? Fuck. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: You're sure about that? ");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: Yes! Please, that's all i know! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Stop screaming, goddamnit");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nSoothing voice: HELP! HEEEL-MMM ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert: Hey, hey, I will pull you up if you quiet the fuck down. ");
         this_thread::sleep_for(chrono::seconds(3));
         cout<<" "<<endl;

     }
    cout<<" A = let him drop"<<endl;
    cout<<" D = Pull him Back"<<endl;
    cin>> w;

    if(w == 'a'|| w == 'A'){
         typewrite("\nRobert: Sir. If you want something to scream about, I'll give you something to scream about");
         this_thread::sleep_for(chrono::seconds(4));
         typewrite("\nDrops him*");
          cout<<" "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
                  cout<<"! He will remember that. "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         isMercyful = false;
         
    }
    else if(w == 'd'|| w == 'D'){
       typewrite("\nRobert: honestly it was nice talking to someone about this stuff. ");
         this_thread::sleep_for(chrono::seconds(4));
                   cout<<"pulls him back* "<<endl;
                   cout<<" "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
                  cout<<"! He will remember that. "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         isMercyful = true;
    }
    else{
      typewrite("\nRobert: Sir. If you want something to scream about, I'll give you something to scream about");
         this_thread::sleep_for(chrono::seconds(4));
         cout<<"Drops him*"<<endl;
         cout<<" "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"! He will remember that. "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         isMercyful = false;
    }
    


}

void news(){
        typewrite("\nVideos flooded social media and our newsroom");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nShowing what many thought to be. . . ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nThe death of Mecha man. . . again. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nFor nearly fifty years ba Mecha man has protected Los Angeles. Built by an ordinary civilian with ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nno superpowers. This first suit was dubbed by some fans as Mecha Man ‘Prime.’");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nThree generations of pilot, their identities kept secret, have given everything to protect this ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\ncommunity. . . Including their lives. After the tragic passing of prime, the next Mecha man, ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nknown as  Astral , teamed up with the star studded superhero group known as the brave brigade, ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nLeading to an unprecedented period of peacr in the southland. After years of loyal service, tragedy struck again when - - ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nShroud, a former brave brigade member turned villian, killed the pulor of Mecha Man in cold blood.");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nBut yet again, as hope was lost, a third generation of Mecha Man emerged. A mecha man for the modern era. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nA bulwark against organized crime in the greated Los angeles area, until, today.");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nEarlier today, Mecha Man broke his silence And addresses the media. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nWalks into the stage in front of dozens of cameras, reporters and lights*");
         this_thread::sleep_for(chrono::seconds(3));
                 typewrite("\nYou: There's been a alot of speculation about my health and the state of the Mecha Man suit, ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nand I'm ready to put that speculation to rest.");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nThe suit has been damages beyond my ability to repair, ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nSo I will be stepping back from superhero work. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nEffective immediately. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nReporter gibberish*");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nYou: one at a time, please. ");
         this_thread::sleep_for(chrono::seconds(2));
                 typewrite("\nReporter1: Do you have anything to say to your fans? ");
                  std::cout<<" "<<endl;
                  std::cout<<"=====CHOICE======"<<endl;
                  std::cout<<"W = Thank you"<<endl;
                  std::cout<<"A = I did my best"<<endl;
                  std::cout<<"D = It was all for you"<<endl;
                 char a;
                  std::cin>>a;
        if(a == 'w' || a== 'W'){
                typewrite("\nEverything I've done, every sacrifice me and my family have made has been for the people of Los Angeles. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nNext question. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Does this mean you're retiring as Mecha Man? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite(" \nWord on the street is you're donezo. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: are you a hundred years old? Why're you talking like that?");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Answer the question, buddy boy. Are you retiring? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nMy readers need the skinny and i am to deliver");
                 std::cout<<" "<<endl;
        }
        else if( a == 'a'|| a== 'A'){
                typewrite("\n I suppose I want them to know I did my best. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n I worked as long as i could as hard as i could and. . .  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n That's all anyone can do.");
                typewrite("\nNext question. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Does this mean you're retiring as Mecha Man? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Word on the street is you're donezo. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: are you a hundred years old? Why're you talking like that?");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Answer the question, buddy boy. Are you retiring? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nMy readers need the skinny and i am to deliver");
                std:: cout<<" "<<endl;

        }
        else if( a == 'w'|| a== 'W'){
                typewrite("\n I just want to say thank you for the support. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n I hope to be worthy of it.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nNext question. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Does this mean you're retiring as Mecha Man? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Word on the street is you're donezo. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: are you a hundred years old? Why're you talking like that?");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Answer the question, buddy boy. Are you retiring? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nMy readers need the skinny and i am to deliver");
                std:: cout<<" "<<endl;
        

        }
        else{
                typewrite("\nEverything I've done, every sacrifice me and my family have made has been for the people of Los Angeles. . . ");
                typewrite("\nNext question. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Does this mean you're retiring as Mecha Man? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Word on the street is you're donezo. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: are you a hundred years old? Why're you talking like that?");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nReporter 2: Answer the question, buddy boy. Are you retiring? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nMy readers need the skinny and i am to deliver");
                 std::cout<<" "<<endl;
        }
        char w;
        std:: cout<<"=====CHOICE====="<<endl;
         std::cout<<"W = No comment "<<endl;
         std::cout<<"A = Pretty much. "<<endl;
         std::cout<<"D = Not yet. "<<endl;
         std::cin>>w;
        if (w == 'w' || w == 'W'){
                typewrite("\n You: I'm not looking to comment on that just yet. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n That's the only skinny I can give you");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Alright, just one more, please. I gotta get back to uh, just, just one more. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Preferably someone from this century");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: So. Shroud kills your father, goes to jail fifteen years, breaks out and immediately");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n dupes you into a trap");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Where he destroys the Mecha man suit and puts you in a coma for months. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: I didn't hear a question in there. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Two parter. First, why didn't shroud kill you? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You haven't been conscious for months. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n It'd be easy money taking you out. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Ok, Shroud wanted the Astral Pulse and Mecha Man gone");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n He got both I'm not sure I mattered much. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Right. You're unimportant. Which leads me to my next question.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Most heroes avenge their family. You, did the opposite. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You killed their legacy");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n How disappointed would your dad be if he were here right now? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Your father, your grandfather, they must he rolling over in their graves. ");
                 std::cout<<" "<<endl;
        }
        else if ( w == 'a' || w == 'A' ){
                typewrite("\n You: well, I dont have a suit, and I don't have any superpowers so, year, short of a miracle. . .  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Pretty much.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 2: Is pretty much the same as definitively? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Pretty much is the same as pretty much, buddy boy. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Alright, just one more, please. I gotta get back to uh, just, just one more. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Preferably someone from this century");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: So. Shroud kills your father, goes to jail fifteen years, breaks out and immediately");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n dupes you into a trap");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Where he destroys the Mecha man suit and puts you in a coma for months. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: I didn't hear a question in there. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Two parter. First, why didn't shroud kill you? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You haven't been conscious for months. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n It'd be easy money taking you out. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Ok, Shroud wanted the Astral Pulse and Mecha Man gone");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n He got both I'm not sure I mattered much. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Right. You're unimportant. Which leads me to my next question.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Most heroes avenge their family. You, did the opposite. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You killed their legacy");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n How disappointed would your dad be if he were here right now? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Your father, your grandfather, they must he rolling over in their graves. ");
                 std::cout<<" "<<endl;
        }
        else if ( w == 'd' || w == 'D'){
                typewrite("\n You: Look, I'm not retiring, Not yet at least.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Not yet. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Pretty much is the same as pretty much, buddy boy. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Alright, just one more, please. I gotta get back to uh, just, just one more. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Preferably someone from this century");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: So. Shroud kills your father, goes to jail fifteen years, breaks out and immediately");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n dupes you into a trap");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Where he destroys the Mecha man suit and puts you in a coma for months. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: I didn't hear a question in there. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Two parter. First, why didn't shroud kill you? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You haven't been conscious for months. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n It'd be easy money taking you out. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Ok, Shroud wanted the Astral Pulse and Mecha Man gone");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n He got both I'm not sure I mattered much. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Right. You're unimportant. Which leads me to my next question.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Most heroes avenge their family. You, did the opposite. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You killed their legacy");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n How disappointed would your dad be if he were here right now? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Your father, your grandfather, they must he rolling over in their graves. ");
                 std::cout<<" "<<endl;
        }
        else{
                 typewrite("\n You: I'm not looking to comment on that just yet. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n That's the only skinny I can give you");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Alright, just one more, please. I gotta get back to uh, just, just one more. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Preferably someone from this century");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: So. Shroud kills your father, goes to jail fifteen years, breaks out and immediately");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n dupes you into a trap");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Where he destroys the Mecha man suit and puts you in a coma for months. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: I didn't hear a question in there. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Two parter. First, why didn't shroud kill you? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You haven't been conscious for months. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n It'd be easy money taking you out. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You: Ok, Shroud wanted the Astral Pulse and Mecha Man gone");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n He got both I'm not sure I mattered much. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Reporter 3: Right. You're unimportant. Which leads me to my next question.  ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Most heroes avenge their family. You, did the opposite. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n You killed their legacy");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n How disappointed would your dad be if he were here right now? ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Your father, your grandfather, they must he rolling over in their graves. ");
                std:: cout<<" "<<endl;
        }
        char c;
        std:: cout<<"=====CHOICE====="<<endl;
        std:: cout<<"W = LEAVE"<<endl;
        std:: cout<<"A = ANSWER"<<endl;
        std:: cout<<"D = ATTACK"<<endl;
        std:: cin>>c;
        if(c =='w' || c == 'W'){
               std::  cout<<"Leaves*"<<endl;
                std:: cout<<"! Everyone will remember that"<<endl;


        }
        else if (c == 'a' || c== 'A'){
                typewrite("\n You: I think he'd be proud. . . Cause I'm alive");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Which, as you is sensitively pointed out, he isn't. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n I think he knows that I sacrificed everything. . . And that i did my best. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Being Mecha man. . . Protecting my community. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Was the greatest honor I'll ever have. . . ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Now I have to live knowing that. ");
                this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Thank you for coming. ");
                this_thread::sleep_for(chrono::seconds(2));
                std:: cout<<"! Everyone will remember that"<<endl;
                blonde_blazerInterest += 1;
        }
        else if( c == 'd'|| c == 'D'){
              std::   cout<<"Attacks him*"<<endl;
                this_thread::sleep_for(chrono::seconds(2));
              std::   cout<<"! Everyone will remember that"<<endl;
                this_thread::sleep_for(chrono::seconds(2));
                std:: cout<<"! Kicks him and beats him repeatedly *"<<endl;
        }
       

}

void streetfight(){
        typewrite("\n Tv: Up next");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Is Mecha Man a Mecha quitter? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Random dude: Yeah, I mean, it seems like it'd be better if he died than be a mecha coward, you know? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n What kind of example if he setting? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Robbers breaking in store*");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         int w;
         cout<<"=====CHOICE====="<<endl;
         cout<<"W = Put it down and leave. "<<endl;
         cout<<"A = that's pretty disrespectful"<<endl;
         cout<<"D = I was watching that. "<<endl;
         cin>>w;
         if(w == 'w' || w == 'W'){
         typewrite("\n Hey, assholes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Goons notices*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah. Just put the TV's back and you can leave. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I won't say anything to the cops and this doesn't need to escalate. ");
          typewrite("\n Red goon: aye! Who're you callin' assholes, asshole!? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOrange goon: I'll handle this idiot. Keep loading. We're out of here in thirty,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And who the fuck are you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah, you. Go go fucking hobo ranger. Who the fuck are you? Choice");
         cout<<" "<<endl;
         }
         else if( w == 'a' || w == 'A'){
         typewrite("\n Hey, assholes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Goons notices*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah. All you assholes. Pretty disrespectful for you to be doing that in front of me. ");
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\n Red goon: aye! Who're you callin' assholes, asshole!? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOrange goon: I'll handle this idiot. Keep loading. We're out of here in thirty,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And who the fuck are you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah, you. Go go fucking hobo ranger. Who the fuck are you? Choice");
         cout<<" "<<endl;
         }
         else if ( w == 'd' || w == 'D'){
         typewrite("\n Hey, assholes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Goons notices*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah. All you assholes. I was fucking watching that. ");
          typewrite("\n Red goon: aye! Who're you callin' assholes, asshole!? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOrange goon: I'll handle this idiot. Keep loading. We're out of here in thirty,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And who the fuck are you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah, you. Go go fucking hobo ranger. Who the fuck are you? Choice");
         cout<<" "<<endl;
         }
         else {
         typewrite("\n Hey, assholes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Goons notices*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah. Just put the TV's back and you can leave. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I won't say anything to the cops and this doesn't need to escalate. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Red goon: aye! Who're you callin' assholes, asshole!? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOrange goon: I'll handle this idiot. Keep loading. We're out of here in thirty,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And who the fuck are you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Yeah, you. Go go fucking hobo ranger. Who the fuck are you? Choice");
         cout<<" "<<endl;
         }
         char d;
         cout<<"=====CHOICE====="<<endl;
         cout<<"W = someone with nothing to lose."<<endl;
         cout<<"A = I'm Mecha Man. "<<endl;
         cout<<"D = Honestly, not sure anymore. "<<endl;
         cin>>d;
         if(d == 'a' || d == 'A'){
         typewrite("\n You: I'm Mecha Man. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Green goon: you're not Mecha Man. That's Mecha Man. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: I'm Mecha Man, dipshit, okay? That's my suit. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Orange goon: well, you're gonna be wishin' you had it right now.");
         this_thread::sleep_for(chrono::seconds(2));
           this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: whatever the case, im enough to deal with a pack of fuckin' skittles idiots like you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Orange goon: Goddamnit. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Look, you're hurt, you're not thinkin' straight.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou don't wanna fight someone who knows which hand every punch is coming from- -  ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if ( d == 'w' || d == 'W'){
         typewrite("\n Someone with nothing to lose. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: Right now, I'm someone with nothing to lose. . .  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Which us probably bad news for you.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Orange goon: Well. What nothing you have, you're about to lose again ");
           this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: whatever the case, im enough to deal with a pack of fuckin' skittles idiots like you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Orange goon: Goddamnit. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Look, you're hurt, you're not thinkin' straight.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou don't wanna fight someone who knows which hand every punch is coming from- -  ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if( d == 'd' || d == 'D'){
         typewrite("\n You: Honestly. . . I'm not sure anymore.  ");
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\n Orange goon: right. Well, that feels heavy, ");
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\n But I don't know you so I don't give a fuck. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: whatever the case, im enough to deal with a pack of fuckin' skittles idiots like you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Orange goon: Goddamnit. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Look, you're hurt, you're not thinkin' straight.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou don't wanna fight someone who knows which hand every punch is coming from- -  ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
        int s;
         cout<<"Note: Left arm is broken"<<endl;
          cout<<"=====CHOICE====="<<endl;
           cout<<"A = Punch with left "<<endl;
            cout<<"D = punch with right"<<endl;
        cin>>s;
        if(s == 'd'|| s == 'D'){
         typewrite("\n Misses punch*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Gets hit by a crowbar*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Receives heavy concussion* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Note: This effect has long term effects throughout the fight”");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Random superhero swoops in for the save*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: That guy I threw. . . He landed on the roof, right?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: All good! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: Phew");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blonde Blazer: Hi. I'm Blonde Blaze");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I work over at Superhero Dispatch Network. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: hey. Hi. Yeah. I know who you are. You're, like, famous. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: Oh. Uh, I don't know about that. I'm just a corporate hero for hire. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're the real deal. You're the famous one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Sees the beat up hooligans");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: right. . . I don't think infamous is the same as famous, but - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: oh, God. Your arm. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: oh, fuck");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hmm? Oh. Yeah. That's gross. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: you look like some kinda mime DJ. Let me see it. I'll help you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Note: she's helping you out by fixing your left arm");
         cout<<" "<<endl;
         blonde_blazerInterest = 1;
         isRobertInjured = true;
        }
        else if (s == 'a' || s == 'A'){
                typewrite("\n Lands punch* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Goon stays down* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n '500+ aura'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Random superhero swoops in for the save*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: That guy I threw. . . He landed on the roof, right?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: All good! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: Phew");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blonde Blazer: Hi. I'm Blonde Blaze");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I work over at Superhero Dispatch Network. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: hey. Hi. Yeah. I know who you are. You're, like, famous. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: Oh. Uh, I don't know about that. I'm just a corporate hero for hire. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're the real deal. You're the famous one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Sees the beat up hooligans");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: right. . . I don't think infamous is the same as famous, but - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: oh, God. Your arm. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: oh, fuck");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hmm? Oh. Yeah. That's gross. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: you look like some kinda mime DJ. Let me see it. I'll help you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Note: she's helping you out by fixing your left arm");
         cout<<" "<<endl;
         blonde_blazerInterest = 3;
         isRobertInjured = false;
        }
        else{
                typewrite("\n Misses punch*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Gets hit by a crowbar*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Receives heavy concussion* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Note: This effect has long term effects throughout the fight”");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Random superhero swoops in for the save*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: That guy I threw. . . He landed on the roof, right?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: All good! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ???: Phew");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blonde Blazer: Hi. I'm Blonde Blaze");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I work over at Superhero Dispatch Network. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: hey. Hi. Yeah. I know who you are. You're, like, famous. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: Oh. Uh, I don't know about that. I'm just a corporate hero for hire. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're the real deal. You're the famous one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Sees the beat up hooligans");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: right. . . I don't think infamous is the same as famous, but - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: oh, God. Your arm. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: oh, fuck");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hmm? Oh. Yeah. That's gross. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Blazer: you look like some kinda mime DJ. Let me see it. I'll help you. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Note: she's helping you out by fixing your left arm");
         cout<<" "<<endl;
          blonde_blazerInterest += 1;
          isRobertInjured = true;
        }
       char r;
        cout<<"=====CHOICE====="<<endl;
       cout<<"W = Have you done this before? "<<endl;
       cout<<"A = I'd prefer a professional. "<<endl;
       cout<<"D = I can do it myself. "<<endl;
       cin>>r;

       if (r == 'a'|| r == 'A'){
        typewrite("\n Blazer: It usually dislocates the other way. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: No offense, but I'd prefer a medical professional to do i - - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Fixes your arm*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Arm now slightly fixed'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: uuuooooowuhhhkay. . . Ohh. That's uh, that's okay. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ' Yeah, that's oh. Okay. Yeah, thank you.  '");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Blazer: You look like you could use a drink.'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: What makes you say that? ");
         this_thread::sleep_for(chrono::seconds(2));
       }
       else if( r == 'd' || r == 'D'){
        typewrite("\n Blazer: It usually dislocates the other way. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: it's fine. No, no it's fine don't worry. I can do it myse- -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Fixes your arm*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Fixes your arm*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Arm slightly fixed'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ' Note: I think? '");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ' You: fuck!  '");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ' Yeah, that's oh. Okay. Yeah, thank you.  '");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Blazer: You look like you could use a drink.'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: What makes you say that? ");
         this_thread::sleep_for(chrono::seconds(2));
       }
       else if (r == 'w'|| r =='W'){
        typewrite("\n Blazer: It usually dislocates the other way. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: okay, but have you done this before? Cause maybe - - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Fixes your arm*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Arm now slightly fixed' ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: Owwwww, wow-jay. Wow, that's okay. Thank you");
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\n ' Yeah, that's oh. Okay. Yeah, thank you.  '");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n 'Blazer: You look like you could use a drink.'");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: What makes you say that? ");
         this_thread::sleep_for(chrono::seconds(2));
       }

}

void Bar(){

         typewrite("\nBlaze: oh shit. They were all skittles colors I didn't even realize it. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: yeah. Well you know what they can taste the rainboe in jail. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: wh- what do you mean? Like different colored dicks? ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"You both laugh* "<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: shit. Sorry. Sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou:  I actually didn't know what i meant, ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n But you know it should've been that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm a little buzzed. I don't really make jokes like that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  I think Visi's rubbing off on me. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: uhh someone I work with, it's, yeah. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"Sees blazer chugs her alcohol drink*"<<endl;
         cout<<" "<<endl;
         char a;
         cout<<"W = that feels like a challange "<<endl;
         cout<<"A = Rough day? "<<endl;
         cout<<"D = Impressive. "<<endl;
         cin>>a;
         if(a == 'a' ||a == 'A'){
          typewrite("\nYou: I can't tell which one of us had the rough day");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: oh. No. I had a great day. Well, that's not totally true. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Oh, you're joking. Sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Don't worry about keeping up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  Something to do with the powers. . . It,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  It takes a lot for me to feel anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: what a sad sentence.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Hey, you should give yourself more credit. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n To do what you did, for as long as you did. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Especially without superpowers. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Not true. Whatddaya call this? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Breaks finger and puts it back like nothing happened*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I can fix that one for you too");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: No thanks. I like this one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So how'd you afford all that? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must've spent hundreds of thousands - - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you'll understand when the check comes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: you're serious. Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, I basically burned though my entire inheritance keeping it going. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Last couple years have just been duct and sheer determination. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Wh- why did you do it? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Do what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Sacrifice everything to be a superhero to the point of financial");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: ruin? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I was gonna say hardship, but ruin was the first word that came to mind, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must be completely broke at this point. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if (a == 'd'|| a == 'D'){
         typewrite("\nYou: That was. . . Impressive. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Don't worry about keeping up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  Something to do with the powers. . . It,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  It takes a lot for me to feel anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: what a sad sentence.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Hey, you should give yourself more credit. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n To do what you did, for as long as you did. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Especially without superpowers. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Not true. Whatddaya call this? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Breaks finger and puts it back like nothing happened*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I can fix that one for you too");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: No thanks. I like this one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So how'd you afford all that? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must've spent hundreds of thousands - - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you'll understand when the check comes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: you're serious. Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, I basically burned though my entire inheritance keeping it going. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Last couple years have just been duct and sheer determination. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Wh- why did you do it? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Do what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Sacrifice everything to be a superhero to the point of financial");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: ruin? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I was gonna say hardship, but ruin was the first word that came to mind, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must be completely broke at this point. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if (a == 'w' || a == 'w'){
          typewrite("\n You: that, feels like a challenge");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: oh. No. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Don't worry about keeping up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  Something to do with the powers. . . It,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  It takes a lot for me to feel anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: what a sad sentence.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Hey, you should give yourself more credit. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n To do what you did, for as long as you did. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Especially without superpowers. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Not true. Whatddaya call this? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Breaks finger and puts it back like nothing happened*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I can fix that one for you too");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: No thanks. I like this one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So how'd you afford all that? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must've spent hundreds of thousands - - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you'll understand when the check comes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: you're serious. Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, I basically burned though my entire inheritance keeping it going. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Last couple years have just been duct and sheer determination. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Wh- why did you do it? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Do what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Sacrifice everything to be a superhero to the point of financial");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: ruin? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I was gonna say hardship, but ruin was the first word that came to mind, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must be completely broke at this point. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else{
          typewrite("\n You: that, feels like a challenge");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: oh. No. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Don't worry about keeping up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  Something to do with the powers. . . It,  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n  It takes a lot for me to feel anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: what a sad sentence.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Hey, you should give yourself more credit. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n To do what you did, for as long as you did. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Especially without superpowers. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Not true. Whatddaya call this? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Breaks finger and puts it back like nothing happened*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I can fix that one for you too");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: No thanks. I like this one. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So how'd you afford all that? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must've spent hundreds of thousands - - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you'll understand when the check comes. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: you're serious. Millions. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, I basically burned though my entire inheritance keeping it going. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Last couple years have just been duct and sheer determination. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Wh- why did you do it? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Do what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Sacrifice everything to be a superhero to the point of financial");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: ruin? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I was gonna say hardship, but ruin was the first word that came to mind, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You must be completely broke at this point. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         cout<<"=====Choice====="<<endl;
         char s;
         cout<<"W = I don't know. . . "<<endl;
         cout<<"A = I like helping people. "<<endl;
         cout<<"D = only thing I'm good at. "<<endl;
         cin>>s;

         if(s == 'd'|| s == 'D'){
          typewrite("\nYou: honestly, it's the only thing I'm good at. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n! Blonde blazer will remember that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Now, that's a sad sentence. Come on, that can't be true. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I guess it could be true.");
         this_thread::sleep_for(chrono::seconds(2));
         blonde_blazerInterest += 1;
         }
         else if (s == 'a'|| s =='A'){
          typewrite("\n I don't know, I guess i just like helping people. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n! Blonde Blazer will remember that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So are you drunk yet, or what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Eh, yeah, I should probably be switching to water. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Good, cause I was hoping after some drinks to loosen you up, ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I could learn a little more about you and uh. . .  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n If it that went well. . . Present you with a proposition. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: a proposition");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Yeah, I'm just trying to figure out if this is a fit or not. You know?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Then it's just a matter of picking the right moment. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         blonde_blazerInterest += 2;
         }
         else if(s == 'D'|| s == 'd'){
          typewrite("\nYou: honestly, it's the only thing I'm good at. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ! Blonde blazer will remember that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Now, that's a sad sentence. Come on, that can't be true. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I guess it could be true.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So are you drunk yet, or what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Eh, yeah, I should probably be switching to water. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Good, cause I was hoping after some drinks to loosen you up, ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I could learn a little more about you and uh. . .  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n If it that went well. . . Present you with a proposition. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: a proposition");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Yeah, I'm just trying to figure out if this is a fit or not. You know?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Then it's just a matter of picking the right moment. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         blonde_blazerInterest += 1;
         }
         else{
          typewrite("\nYou: honestly, it's the only thing I'm good at. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n ! Blonde blazer will remember that. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Now, that's a sad sentence. Come on, that can't be true. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I guess it could be true.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: So are you drunk yet, or what? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Eh, yeah, I should probably be switching to water. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Good, cause I was hoping after some drinks to loosen you up, ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I could learn a little more about you and uh. . .  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n If it that went well. . . Present you with a proposition. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You: a proposition");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Yeah, I'm just trying to figure out if this is a fit or not. You know?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Then it's just a matter of picking the right moment. ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         blonde_blazerInterest += 1;
         }
         char d;
         cout<<"=====CHOICE====="<<endl;
         cout<<"W = Hopefully nothing scandalous. "<<endl;
         cout<<"A = Sounds intriguing. "<<endl;
         cout<<"D = A bar proposition? "<<endl;
         cin>>d;
         if(d == 'w' || d == 'W'){
          typewrite("\nYou: Okay. . . Hopefully it's nothing too. Scandalous. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDrinks*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: that's actually- - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Not water.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: This is like pure alcohol*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's not like alcohol, it is alcohol. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I know because you spit it directly into my open mouth");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: you drink hard liquor straight from a pint glass? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: actually, I usually just drink it straight from the bottle but. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I thought that would be embarrassing. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I'm gonna go clean up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: I'm so sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : hey bitch.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hey! I'm talking to you bitch! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Alright just so you know, I only turned around cause someone yelled, not because I'm a ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n bitch or anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : don't you watch the news? This is a superhero bar. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Oh, wow, are you a superhero? That's so cool. Is this your power?  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're like, tell people obvious shit guy? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : you're really gonna act like you don't remember me? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait it's coming back to me. . . Hold on, yeah, it's coming back to me. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Oh, wait, wait, oh I know it. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if(d == 'a'|| d =='A'){
          typewrite("\nYou: Well, I have to say Blazer, that does sound intriguing. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDrinks*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: that's actually- - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSpits the drink on blazer*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Not water.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: This is like pure alcohol*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's not like alcohol, it is alcohol. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I know because you spit it directly into my open mouth");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: you drink hard liquor straight from a pint glass? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: actually, I usually just drink it straight from the bottle but. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I thought that would be embarrassing. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I'm gonna go clean up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: I'm so sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : hey bitch.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hey! I'm talking to you bitch! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Alright just so you know, I only turned around cause someone yelled, not because I'm a ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n bitch or anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : don't you watch the news? This is a superhero bar. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Oh, wow, are you a superhero? That's so cool. Is this your power?  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're like, tell people obvious shit guy? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : you're really gonna act like you don't remember me? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait it's coming back to me. . . Hold on, yeah, it's coming back to me. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Oh, wait, wait, oh I know it. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if(d == 'd'|| d == 'D'){
          typewrite("\nYou: well, a propositions late at night in a bar can go any number of ways.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDrinks*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: that's actually- - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSpits the drink on blazer*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Not water.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: This is like pure alcohol*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's not like alcohol, it is alcohol. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I know because you spit it directly into my open mouth");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: you drink hard liquor straight from a pint glass? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: actually, I usually just drink it straight from the bottle but. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I thought that would be embarrassing. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I'm gonna go clean up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: I'm so sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : hey bitch.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hey! I'm talking to you bitch! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Alright just so you know, I only turned around cause someone yelled, not because I'm a ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n bitch or anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : don't you watch the news? This is a superhero bar. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Oh, wow, are you a superhero? That's so cool. Is this your power?  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're like, tell people obvious shit guy? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : you're really gonna act like you don't remember me? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait it's coming back to me. . . Hold on, yeah, it's coming back to me. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Oh, wait, wait, oh I know it. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else{
         typewrite("\nYou: well, a propositions late at night in a bar can go any number of ways.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDrinks*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: that's actually- - ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSpits the drink on blazer*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Not water.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: This is like pure alcohol*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's not like alcohol, it is alcohol. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I know because you spit it directly into my open mouth");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: you drink hard liquor straight from a pint glass? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: actually, I usually just drink it straight from the bottle but. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I thought that would be embarrassing. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n I'm gonna go clean up. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: I'm so sorry. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : hey bitch.. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Hey! I'm talking to you bitch! ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Alright just so you know, I only turned around cause someone yelled, not because I'm a ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n bitch or anything. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : don't you watch the news? This is a superhero bar. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Oh, wow, are you a superhero? That's so cool. Is this your power?  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're like, tell people obvious shit guy? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n??? : you're really gonna act like you don't remember me? ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait it's coming back to me. . . Hold on, yeah, it's coming back to me. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Oh, wait, wait, oh I know it. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         cout<<"=====Choice====="<<endl;
         char w;
         cout<<"W = Dr. Don't do shit"<<endl;
         cout<<"A = Mr. Wet Ponytail"<<endl;
         cout<<"D = Sunglasses at Night guy"<<endl;
         cin>>w;

         if(w == 'D'|| w =='d'){
          typewrite("\nYou : You are sunglasses, Indoors, at Night guy. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Your superpower is that you're able to let everyone know in the room that you're a douchebag");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Cause you should literally never do that unless you're at the final table of the world Series of ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n poker - - - but even then, if you win, you're still a fucking loser. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: No, Mecha Bitch, I control the fire- - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: the fire?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: AND the flame. . . and my skin does not burn. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: I am Flambae, and you - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait, oh shit I do remember you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're that shitty villain I busted up.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: Not anymore, Mecha Dick.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n As I literally just said, I am flambae, a real superhero.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And you are not Mechaman anymore");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you need to get the fuck out of here.");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
        else if(w == 'a'|| w == 'A'){
          typewrite("\nYou: you know what, you are Mr. Wet Ponytail. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYour superpower is in, obviously, your pony tail, which, aside from being disgust");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n kills themself.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: No, Mecha Bitch, I control the fire- - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: the fire?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: AND the flame. . . and my skin does not burn. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: I am Flambae, and you - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait, oh shit I do remember you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're that shitty villain I busted up.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: Not anymore, Mecha Dick.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n As I literally just said, I am flambae, a real superhero.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And you are not Mechaman anymore");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you need to get the fuck out of here.");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
        }
        else if(w == 'W'|| w == 'w'){
          typewrite("\n You are Dr. Don't Do shit.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Your deal is you walk up to people, look threatening, talk a big game then don’t do shit about it.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n Also, you're not a real doctor cause you're a fuckin idiot.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: No, Mecha Bitch, I control the fire- - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: the fire?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n???: AND the flame. . . and my skin does not burn. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: I am Flambae, and you - -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: wait, oh shit I do remember you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n You're that shitty villain I busted up.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: Not anymore, Mecha Dick.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n As I literally just said, I am flambae, a real superhero.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n And you are not Mechaman anymore");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\n So you need to get the fuck out of here.");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
        }
        cout<<"Note: Choose which bottle to throw him with."<<endl;
        char q;
        cout<<"=====CHOICE====="<<endl;
        cout<<"A = [WATER}"<<endl;
        cout<<"D ={ALCOHOL}"<<endl;
        cin>>q;
        if(q == 'a' || q == 'Q'){
          cout<<"Throws him water*"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"! Everyone will remember that"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: there easy way");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae: I've been waiting a long time for this.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nFlambae throws a punch*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSlips through the water you threw him with*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nHits his teeth in the counter and loses some of it*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nGets knocked out*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nShe comforts you and brings you somewhere else*");
         
         
        }
        else if(q == 'd' || q == 'D'){
         cout<<"Throws him pure alcohol*"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"! Everyone will remember that"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\nFire spreads throughout his body*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nHe panics and runs*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nStore kicks you out*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nShe comforts you and brings you somewhere else*");
         Flambae_Anger_Level += 3;
        }
        else{
          cout<<"Throws him pure alcohol*"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
         cout<<"! Everyone will remember that"<<endl;
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\nFire spreads throughout his body*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nHe panics and runs*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nStore kicks you out*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nShe comforts you and brings you somewhere else*");
         Flambae_Anger_Level += 5;
         blonde_blazerInterest += 3;
        
        }
      }

void Billboard(){
         typewrite("\nYou: I actually thought you were taking us to sit on it.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Hmm? Oh yeah, That woulda been cool.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: yeah. Well you know what they can taste the rainbow in jail. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut , no, yeah, this, this is better cause you can see it.. And it,");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIt looks like there's a couple up there already so.");
         this_thread::sleep_for(chrono::seconds(2));
        std:: cout<<"=====CHOICE====="<<endl;
         char e;
         cout<<"A - Thank for helping me."<<endl;
         cout<<"W - I really needed this. "<<endl;
         cout<<"D - Sorry about the spit thing."<<endl;
         cin>>e;
         if(e == 'w' || e =='W'){
          typewrite("\nDrinking*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: All this been pretty… overwhelming, you know.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nI don't get out and… I think I really needed this.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSo, thank you… for bringing me out. I had a really great time.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Don't worry about it, Robert Robertson.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: How do you know my name?");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nBlazer: Whoops.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: What's going on?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer:Okay I'II tell you, but only after you tell me how you ended up with a name like that.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: oh you mean a name like Robert Robertson the Third?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: The Third?! Three times this happened?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: There was grandpa Bobby, Then my dad, Who i called dad, but every one else called ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobbie, and then me,");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert Cause I wanted to be taken seriously. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Mmm, a family tradition.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: In more ways than one.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nGramps was the first Mecha Man, he died in the suit before I was born.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDad was killed fighting Shroud. That's when I took over. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThere was a little inheritance and the life insurance money.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm so sorry, I didn't realize. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Just your run of the mill, sad superhero origin story.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThe family tradition, if there is one, is dying in that suit.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhich I guess I don't have to worry about anymore.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhat is it?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm not quite drunk yet to share my origin story just yet. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut, remind me to tell you someday.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Definitely");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer hold your chin*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: We can work with this.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if (e =='d' || e =='D'){
          typewrite("\nYou: I know I shouldn't even bring this up cause it was mortifying for the both of us, I am ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nseriously sorry about the whole spitting thing.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSo, thank you… for bringing me out. I had a really great time.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Yeah, that's usually like a second date kinda thing with me.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm joking. I'm joking.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: No, totally, I was thinking about something else. But yeah. Thanks");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThank you. Again.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThis is, this is not a date.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Don't worry about it, Robert Robertson.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: How do you know my name?");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nBlazer: Whoops.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: What's going on?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer:Okay I'II tell you, but only after you tell me how you ended up with a name like that.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: oh you mean a name like Robert Robertson the Third?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: The Third?! Three times this happened?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: There was grandpa Bobby, Then my dad, Who i called dad, but every one else called ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobbie, and then me,");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert Cause I wanted to be taken seriously. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Mmm, a family tradition.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: In more ways than one.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nGramps was the first Mecha Man, he died in the suit before I was born.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDad was killed fighting Shroud. That's when I took over. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThere was a little inheritance and the life insurance money.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm so sorry, I didn't realize. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Just your run of the mill, sad superhero origin story.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThe family tradition, if there is one, is dying in that suit.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhich I guess I don't have to worry about anymore.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhat is it?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm not quite drunk yet to share my origin story just yet. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut, remind me to tell you someday.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Definitely");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer hold your chin*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: We can work with this.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         else if(e == 'A' || e =='a'){
          typewrite("\nYou:I really appreciate you coming to my rescue back there.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nI still think I could've handled it but- all I'm trying to say is…");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThanks . I should've just said that. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut, yep I'm still talking.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Don't worry about it, Robert Robertson.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: How do you know my name?");
         this_thread::sleep_for(chrono::seconds(3));
         typewrite("\nBlazer: Whoops.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: What's going on?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer:Okay I'II tell you, but only after you tell me how you ended up with a name like that.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: oh you mean a name like Robert Robertson the Third?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: The Third?! Three times this happened?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: There was grandpa Bobby, Then my dad, Who i called dad, but every one else called ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobbie, and then me,");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert Cause I wanted to be taken seriously. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Mmm, a family tradition.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: In more ways than one.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nGramps was the first Mecha Man, he died in the suit before I was born.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nDad was killed fighting Shroud. That's when I took over. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThere was a little inheritance and the life insurance money.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm so sorry, I didn't realize. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Just your run of the mill, sad superhero origin story.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThe family tradition, if there is one, is dying in that suit.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhich I guess I don't have to worry about anymore.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhat is it?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm not quite drunk yet to share my origin story just yet. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut, remind me to tell you someday.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Definitely");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer hold your chin*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: We can work with this.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer removed your mask/helmet* ");
         this_thread::sleep_for(chrono::seconds(2));
         cout<<" "<<endl;
         }
         char a;
         cout<<"=====CHOICE====="<<endl;
          cout<<"A - [KISS HER] "<<endl;
           cout<<"D - [LET THE MOMENT PASS]"<<endl;
           cin>>a;
        if(a == 'a'|| a == 'A'){
          typewrite("\nYou kiss her* ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nShe pulls away*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: No. This.. I'm, I'm. . sorry if I gave the wrong impression. . . ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Oh no.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's my fault.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: I'm so sorry. I totally misread that- -");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: You didn't- I mean, you did, but I definitely... ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThe way I went about things tonight… I'm sorry.  ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIt was all a little loose. A little unprofessional. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Unprofessional? I'm not sure that that applies to this but-");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nCuts you off*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm actually here on official business… how i know your name, the proposition i ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nmentioned right before you spit in my mouth— ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Still mortified about that.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Do you remember a superhero named Track Star?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Remember? Of course. He's like a family… that I haven't talked to in a while.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhat about him?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I work with him at SDN. He recommended you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nMention that he worked with your father?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, he was the youngest member of the brave Brigade so, he was kind of my babysitter ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nfor a while. How's he doing?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: He's doing great. He's, he's why I'm here.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert, I would like to make you an offer.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIn exchange for dispatching and mentoring some of our rookie heroes, SDN can make you ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nMecha Man again.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut this time with all the resources that a company as big as our has at their disposal.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nNo more paying out of pocket for repairs.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSupport staff, the whole shebang.");
         this_thread::sleep_for(chrono::seconds(2));
          typewrite("\nYou: So, when do we get started?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOh. Right. I was suppose to test you at the bar. Here.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer throw something to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Eh, Drunk people shouldn't be throwing things at drunk people.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Noted.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou go through a playthrough*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: That felt pretty natural to me...");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIn that it's natural for me to end up mid tier.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThat said, for the first time it feels pretty…");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou look at Blazer and wake her up*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Did I drool on you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Uh, I don't think so.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Well, you see why we thought of- well, why Track Star thought of you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: It makes sense, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer fly off the billboard and looks to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Meet me at the SDN office tomorrow.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nI'm not done saving you yet.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer fly off*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou look down*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Uh, is there a ladder? Or...");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou're back home and your dog runs to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Sorry I'm late.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nPets the dog*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nepisode 1 Ends....");
         this_thread::sleep_for(chrono::seconds(2));
         isRomanticTension = true;
        }
        else{
          typewrite("\nAwkward moment");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Sorry, I'm actually here with. . . a purpose and. . .");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIt was all a little loose. A little unprofessional. ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Unprofessional? I'm not sure that that applies to this but-");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nCuts you off*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I'm actually here on official business… how i know your name, the proposition i ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nmentioned right before you spit in my mouth— ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Still mortified about that.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: It's fine. Do you remember a superhero named Track Star?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Remember? Of course. He's like a family… that I haven't talked to in a while.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nWhat about him?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: I work with him at SDN. He recommended you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nMention that he worked with your father?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Yeah, he was the youngest member of the brave Brigade so, he was kind of my babysitter ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nfor a while. How's he doing?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: He's doing great. He's, he's why I'm here.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nRobert, I would like to make you an offer.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIn exchange for dispatching and mentoring some of our rookie heroes, SDN can make you ");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nMecha Man again.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBut this time with all the resources that a company as big as our has at their disposal.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nNo more paying out of pocket for repairs.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nSupport staff, the whole shebang.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: So, when do we get started?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nOh. Right. I was suppose to test you at the bar. Here.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer throw something to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Eh, Drunk people shouldn't be throwing things at drunk people.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Noted.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou go through a playthrough*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: That felt pretty natural to me...");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nIn that it's natural for me to end up mid tier.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nThat said, for the first time it feels pretty…");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou look at Blazer and wake her up*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Did I drool on you?");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Uh, I don't think so.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Well, you see why we thought of- well, why Track Star thought of you.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: It makes sense, yeah.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer fly off the billboard and looks to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer: Meet me at the SDN office tomorrow.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nI'm not done saving you yet.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nBlazer fly off*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou look down*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Uh, is there a ladder? Or...");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou're back home and your dog runs to you*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nYou: Sorry I'm late.");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nPets the dog*");
         this_thread::sleep_for(chrono::seconds(2));
         typewrite("\nepisode 1 Ends....");
         this_thread::sleep_for(chrono::seconds(2));
         isRomanticTension = false;
        }
}

void warehouse(){

        typewrite("\nSuiting upp. . .");
        this_thread::sleep_for(chrono::seconds(3));
        std::cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
                 std::cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
                std:: cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
              std::   cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
               typewrite("\nEntering mech. . .");
        this_thread::sleep_for(chrono::seconds(2));
                std:: cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
              typewrite("\nInvading warehouse . . ");
        this_thread::sleep_for(chrono::seconds(2));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
             std::    cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
              std::   cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
              std::   cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
                typewrite("\nBeat up some hooligans*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEntering Boss room. . .");
        this_thread::sleep_for(chrono::seconds(3));
               std::  cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
               std::  cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
               std::  cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
               std::  cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(1));
                typewrite("\nSees cctv footages*");
        this_thread::sleep_for(chrono::seconds(2));
        cout<<" "<<endl;
        char a, w, c;
        std:: cout<<" W = Prepare to die."<<endl;
        std:: cout<<" A = it's over, Shroud."<<endl;
        std:: cout<<" D = I'm here to fuck you up."<<endl;
        std:: cin>> a;
        
        if(a == 'a' || a == 'A'){
                    typewrite("\nYou: Hey, um Can I get a copy of that video when we're through here? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nCause it's over, Shroud. . . ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nnand it'd be nice to watch that back at a different angle.");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? : oh no. It's over, Shroud! ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? : All this buildup. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? turns around*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: Face to face with your father's killer and you come in here with that lame shit?");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou: Ugh. Where the fuck is shroud? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: He'll be here in a bit");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nOh, hey, after our conversation, I asked him about your dad. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nFun little tidbit, he was very well hydrated. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nIt turns out right after shroud shot him in the chest, your dad pissed his pants. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nBetcha didn't know that, huh? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSo, after that bullet tore through his insides, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nHe still had enough time to know how absolutely fucked he was, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nAnd just pissed himself like a little bitch. It was like, so much piss, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\ncommands mech*");
                        cout<<" "<<endl;
        }
        else if ( a == 'w' || a == 'W'){
                                typewrite("\nYou: Hey, um Can I get a copy of that video when we're through here?");
        this_thread::sleep_for(chrono::seconds(2));
                                typewrite("\nCause there are some people out there who're gonna want proof that you're dead.");
        this_thread::sleep_for(chrono::seconds(2));
                                typewrite("\n??? : oh no. Should I update my Wikipedia page? ");
        this_thread::sleep_for(chrono::seconds(2));
                                typewrite("\n??? : All this buildup. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? turns around*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: Face to face with your father's killer and you come in here with that lame shit?");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou: Ugh. Where the fuck is shroud? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: He'll be here in a bit");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nOh, hey, after our conversation, I asked him about your dad. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nFun little tidbit, he was very well hydrated. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nIt turns out right after shroud shot him in the chest, your dad pissed his pants. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nBetcha didn't know that, huh? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSo, after that bullet tore through his insides, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nHe still had enough time to know how absolutely fucked he was, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nAnd just pissed himself like a little bitch. It was like, so much piss, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\ncommands mech*");
                        cout<<" "<<endl;
        }
        else if ( a == 'd' || a == 'd'){
                    typewrite("\nYou: Hey, um Can I get a copy of that video when we're through here?");
        this_thread::sleep_for(chrono::seconds(2));
                    typewrite("\nCause I'm about to fuck you up and those videos do numbers. ");
        this_thread::sleep_for(chrono::seconds(2));
                    typewrite("\nMaybe we can go viral - - ");
        this_thread::sleep_for(chrono::seconds(2));
                    typewrite("\n???: Ohhh, Maybe we can go virallll. . .");
        this_thread::sleep_for(chrono::seconds(2));
                    typewrite("\n??? : All this buildup. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? turns around*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: Face to face with your father's killer and you come in here with that lame shit?");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou: Ugh. Where the fuck is shroud? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: He'll be here in a bit");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nOh, hey, after our conversation, I asked him about your dad. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nFun little tidbit, he was very well hydrated. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nIt turns out right after shroud shot him in the chest, your dad pissed his pants. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nBetcha didn't know that, huh? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSo, after that bullet tore through his insides, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nHe still had enough time to know how absolutely fucked he was, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nAnd just pissed himself like a little bitch. It was like, so much piss, ");
        this_thread::sleep_for(chrono::seconds(2));
                         typewrite("\ncommands mech*");
                         cout<<" "<<endl;

        }
        else{
                         typewrite("\nYou: Hey, um Can I get a copy of that video when we're through here? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nCause it's over, Shroud. . . ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nnand it'd be nice to watch that back at a different angle.");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? : oh no. It's over, Shroud! ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? : All this buildup. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n??? turns around*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: Face to face with your father's killer and you come in here with that lame shit?");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou: Ugh. Where the fuck is shroud? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSoothing voice: He'll be here in a bit");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nOh, hey, after our conversation, I asked him about your dad. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nFun little tidbit, he was very well hydrated. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nIt turns out right after shroud shot him in the chest, your dad pissed his pants. ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nBetcha didn't know that, huh? ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nSo, after that bullet tore through his insides, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nHe still had enough time to know how absolutely fucked he was, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nAnd just pissed himself like a little bitch. It was like, so much piss, ");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\ncommands mech*");
                        cout<<" "<<endl;
        }
      std::   cout <<" A = Punt"<<endl;
       std::  cout <<" D = Stomp"<<endl;
       std::  cin>> w;
        if (w == 'a' || w == 'A'){
                typewrite("Cause your dad was just wearing shorts so it just--");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Hey. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI get it. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLands mech* ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nKicks him epically, Sending him through the rooftop*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n ''Soothing voice gets injured pretty bad'' ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEnters mech and powers up menacingly*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n100+ aura ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Enters boss fight. . . ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: starting to think I didn't need to waste a perfectly good mattress to break your fall.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice: Perfectly good? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI assumed that cum-stained thing was already in the dumpster- -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Can we just skip to the part where you reveal your bullshit superpower so we can fight for real? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice transforms* " );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCompletely naked* ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Cool dick.   So, you, uh, got some kinda clever supervillain name? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Toxic: you can call me toxic. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Bro, that works on so many levels for you. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: yeah, no shit, that's the point. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Fight begins. . . ");
        this_thread::sleep_for(chrono::seconds(2));
          std::       cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                 std::        cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                 std::        cout<<"."  <<endl;
        this_thread::sleep_for(chrono::seconds(2));
                std::         cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou get ambushed and outnumbered*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n Mech gets stuck and heavily damaged*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Shroud shows up*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou activated shield*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Good news, buddy. Shroud says he just wants the Astral Pulse. Which, we all know, isn't ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n really yours anyway. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nShield energy depleting*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCalculating escape plan*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Oh, hey. Boss man says if you're planning on diverting puss pow- - -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nsorry, pulse power to boosters, it won't work.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nThis is a limited time offer dude what's up? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou were so fucking talkative earlier? ");
                cout<<" "<<endl;
                isMercyful = true;
        }
        else if( w == 'd' || w == 'D'){
                typewrite("\nCause your dad was just wearing shorts so it just--");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Hey. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI get it. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nStomps him with mech menacingly*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n ''Soothing voice gets injured awfully bad''  ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEnters mech and powers up menacingly*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n “670+ aura”" );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEnters boss fight. . . ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: starting to think I didn't need to waste a perfectly good mattress to break your fall.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice: Perfectly good? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI assumed that cum-stained thing was already in the dumpster- -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Can we just skip to the part where you reveal your bullshit superpower so we can fight for real? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice transforms* "  );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCompletely naked* ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Cool dick.   So, you, uh, got some kinda clever supervillain name? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Toxic: you can call me toxic. " );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Bro, that works on so many levels for you. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: yeah, no shit, that's the point. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Fight begins. . . "  );
        this_thread::sleep_for(chrono::seconds(2));
           std::      cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
              std::           cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                std::         cout<<"."  <<endl;
        this_thread::sleep_for(chrono::seconds(2));
              std::           cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou get ambushed and outnumbered*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nMech gets stuck and heavily damaged*" );
        this_thread::sleep_for(chrono::seconds(2));
               typewrite("\n Shroud shows up*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou activated shield*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Good news, buddy. Shroud says he just wants the Astral Pulse. Which, we all know, isn't ");
        this_thread::sleep_for(chrono::seconds(1));
                typewrite("\n really yours anyway. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nShield energy depleting*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCalculating escape plan*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Oh, hey. Boss man says if you're planning on diverting puss pow- - -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nsorry, pulse power to boosters, it won't work.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nThis is a limited time offer dude what's up? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou were so fucking talkative earlier? ");
                cout<<" "<<endl;
                isMercyful = false;
        }
        else{
           typewrite("Cause your dad was just wearing shorts so it just--");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Hey. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI get it. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLands mech* ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nKicks him epically, Sending him through the rooftop*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n ''Soothing voice gets injured pretty bad'' ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEnters mech and powers up menacingly*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n “100+ aura” ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Enters boss fight. . . ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: starting to think I didn't need to waste a perfectly good mattress to break your fall.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice: Perfectly good? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI assumed that cum-stained thing was already in the dumpster- -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Can we just skip to the part where you reveal your bullshit superpower so we can fight for real? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Soothing voice transforms* " );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCompletely naked* ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Cool dick.   So, you, uh, got some kinda clever supervillain name? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Toxic: you can call me toxic. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: Bro, that works on so many levels for you. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: yeah, no shit, that's the point. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Fight begins. . . ");
        this_thread::sleep_for(chrono::seconds(2));
          std::       cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                 std::        cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                 std::        cout<<"."  <<endl;
        this_thread::sleep_for(chrono::seconds(2));
                std::         cout<<"."<<endl;
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nYou get ambushed and outnumbered*");
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\n Mech gets stuck and heavily damaged*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n Shroud shows up*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou activated shield*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Good news, buddy. Shroud says he just wants the Astral Pulse. Which, we all know, isn't ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\n really yours anyway. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nShield energy depleting*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nCalculating escape plan*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Oh, hey. Boss man says if you're planning on diverting puss pow- - -");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nsorry, pulse power to boosters, it won't work.");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nThis is a limited time offer dude what's up? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou were so fucking talkative earlier? ");
                cout<<" "<<endl;
                isMercyful = true;
        }
       std:: cout<<"W = Ignore him"<<endl;
       std::  cout<<"A = Bullshit"<<endl;
        std:: cout<<"D = you're bluffing."<<endl;
        std:: cin>>c;
        if(c == 'w' || c == 'W'){
                typewrite("\nI'm just a messenger here. . .");
        this_thread::sleep_for(chrono::seconds(2));

        }
        else if ( c == 'a' || c == 'A'){
                typewrite("\nYou: Bullshit "  );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: hey, mathblaster. i don't give a fuck, okay? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI'm just the messenger here. . . " );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nHe says you're not Calculating that shit stuck to your leg. . . Dumbass. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: fuck it"  );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLaunches boosters to max power*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Aye. Great. All going to plan. Now get outta there, babe. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLaunches it*" );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEscapes. . . ");
        this_thread::sleep_for(chrono::seconds(2));

        }
        else if(c == 'd' || c == 'D') {
                        cout<<"You: you're bluffing"  <<endl;
        this_thread::sleep_for(chrono::seconds(2));
                        typewrite("\nToxic: hey, mathblaster. i don't give a fuck, okay? ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nI'm just the messenger here. . . " );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nHe says you're not Calculating that shit stuck to your leg. . . Dumbass. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nYou: fuck it"  );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLaunches boosters to max power*");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nToxic: Aye. Great. All going to plan. Now get outta there, babe. ");
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nLaunches it*" );
        this_thread::sleep_for(chrono::seconds(2));
                typewrite("\nEscapes. . . ");
        this_thread::sleep_for(chrono::seconds(5));

        }
                        cout<<". "  <<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "  <<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<"Warning! "  <<endl;
        this_thread::sleep_for(chrono::seconds(4));
                        cout<<"Warning! "<<endl;
        this_thread::sleep_for(chrono::seconds(4));
                        cout<<"System: foreign object detected"  <<endl;
        this_thread::sleep_for(chrono::seconds(4));
                        cout<<"Explodes* "<<endl;
        this_thread::sleep_for(chrono::seconds(6));
                        cout<<". "  <<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "<<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "  <<endl;
        this_thread::sleep_for(chrono::seconds(3));
                        cout<<". "<<endl;
        typewrite("\n Welcome to Dispatch");

}

void epilouge_summary(){
 if(isMercyful){
  cout<<"You have skills compassionate leadership."<<endl;
 }
 else{
cout<<"you have skills pragmatic efficiency."<<endl;
 }
 if(isRobertInjured){
 cout<<"You got injured during the streetfight"<<endl;
 cout<<"slowness debuff ineffect until healed by certain scenerio"<<endl;
 }
 else{
  cout<<" "<<endl;
 }
 if (blonde_blazerInterest >= 6){
  cout<<"Blazer views you highly"<<endl;
}
else{
  cout<<"Blazer doesn't have interest in you that much....";
}
if(isRomanticTension){
  cout<<"possible future romantic interactions"<<endl;
}
else{
  cout<<"considered friends and acquintances for the moment."<<endl;
}
if(Flambae_Anger_Level >= 3){
  cout<<"He's very pissed at you"<<endl;
  cout<<"high Possibilty future tension*"<<endl;
}
else{
  cout<<"He's pissed at you"<<endl;
  cout<<"Possible future tension*"<<endl;
}// this shi took me 3 nights to do 3k lines means it's all sweat and blood, hope yall dont think it's ai or imma crash
}
